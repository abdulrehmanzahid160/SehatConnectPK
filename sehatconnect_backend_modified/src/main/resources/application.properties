spring.application.name=SehatConnect

# Render sets the PORT env variable; fall back to 8081 for local dev
server.port=${PORT:8081}

# Bind to all interfaces (required for Render)
server.address=0.0.0.0

# Directory where JSON data files and report .txt files are stored
# Change this path if you want data saved elsewhere
app.data.dir=data
